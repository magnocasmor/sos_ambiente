import 'package:flutter/material.dart';

class ReportMapScreen extends StatelessWidget {
  const ReportMapScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
